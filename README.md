# Jenkins Woodstox API Plugin

This plugin provides woodstox XML parser for other plugins (https://github.com/fasterxml/woodstox)

# Build

To build the plugin locally:

```
mvn -Dset.changelist clean install
```
